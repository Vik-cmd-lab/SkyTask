name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
      - run: |
          pyinstaller --onefile --name SkyTask --add-data "templates;templates" --add-data "static;static" main.py
      - uses: actions/upload-artifact@v4
        with:
          name: SkyTask-windows-ENG
          path: dist/SkyTask.exe
